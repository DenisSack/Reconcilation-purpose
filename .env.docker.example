# Copier vers .env.docker (gitignored si ajouté) ou exporter dans le shell.
# Utilisé par docker-compose.yml pour AUTH_SECRET uniquement — la DB est codée localement comme TEST.

AUTH_SECRET="A3+pJDOhEa9aWLq4/buWzLDQERkRy1LAclLRPGkxxy0="

# Point d’accès navigateur après docker compose up (gateway nginx sur 3080).
# Si vous passez uniquement par web:3000 sans nginx, utilisez http://localhost:3080 après gateway.
# Préférer 127.0.0.1 sous Windows pour éviter les soucis localhost / IPv6.
NEXTAUTH_URL=http://127.0.0.1:3080
